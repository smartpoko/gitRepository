server:
  port: 7911
user:
  servicePath: http://localhost:7901/user/

spring:
  application:
    name: ms-comsumer-order
eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka
  instance:
    instanceId: ${spring.application.name}:${server.port}
      
      
      